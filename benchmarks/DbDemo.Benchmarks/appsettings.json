{
  "ConnectionStrings": {
    "LibraryDb": "Server=${DB_HOST},${DB_PORT};Database=${DB_NAME};User Id=${SA_USER};Password=${SA_PASSWORD};TrustServerCertificate=True;",
    "LibraryDbAppUser": "Server=${DB_HOST},${DB_PORT};Database=${DB_NAME};User Id=${APP_USER};Password=${APP_PASSWORD};TrustServerCertificate=True;"
  }
}
