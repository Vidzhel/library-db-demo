# Database Configuration
# Copy this file to .env and set your own values
# These values will be used by:
# - appsettings.json (for .NET applications)
# - docker-compose.yml (for SQL Server container)

# SQL Server Connection Settings
DB_HOST=localhost
DB_PORT=1453

# SQL Server SA (Admin) User - Used for migrations and setup
# Note: SA user is pre-created by SQL Server. Password is set via docker-compose.
SA_USER=sa
SA_PASSWORD=YourStrong@Passw0rd

# Library Database Settings
DB_NAME=LibraryDb

# Application User - Used by the application for normal operations
# This user is created by the init.sql script with permissions for the application.
APP_USER=library_app_user
APP_PASSWORD=LibraryApp@2024
